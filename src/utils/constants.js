// Swiper slider settings
export const configSwiperSlider = {
  // direction: 'vertical',
  // freeMode: true,
  speed: 2400,
  parallax: true,
  spaceBetween: 18,
  mousewheel: {
    enabled: true,
    sensitivity: 1.8
  }
};

// selectors
export const selectors = {
  particles: '.particles',
  slider: '.slider'
};
